1. We used “IntelliJ IDEA” JAVA IDE as our coding environment. The library we used are already put in lib folder. it can run by IntelliJ or just use the traditional command line.

2. The sample inputs are “show.json”, “genre.txt”, and “net.txt”.